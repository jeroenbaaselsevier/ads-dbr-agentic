{# AUTO-GENERATED — DO NOT EDIT
   Source: agent-core/ + platform/codex/templates/project-resources.SKILL.md.j2
   Regenerate: python scripts/build_agent_adapters.py
#}
---
name: project-resources
description: >
  Project lifecycle management: bootstrap, path derivation, scaffolding,
  session closeout, and lesson capture for analytics projects.
applyTo: "projects/**"
---

# Project Resources Skill

> AUTO-GENERATED. Canonical role definition lives in `agent-core/roles/project-resources.md`.

## At session start

Read `agent-core/roles/project-resources.md` for the full specification.

## Quick reference

### Bootstrap
```bash
python scripts/init_project.py --year 2026 --iso3 NLD --short-name journal_trend --display-name "..."
```

### Activate existing project
```bash
python scripts/activate_project.py --project-id 2026_NLD_journal_trend
```

### Session closeout
1. `python scripts/closeout_project.py --project-id ... --session-id ... --session-status completed`
2. `python scripts/capture_lessons.py --project-id ... --session-id ... --from-json` (if lessons)
3. `python scripts/triage_lessons.py` (periodic)

## Path derivation

`project.yaml` is the single source of truth for all paths.

## Deployment

`deploy.sh` reads the nearest `project.yaml` to derive remote paths automatically.
```bash
./deploy.sh projects/<project_id>/notebooks/spark/<name>.py --run
```
